{
  "name": "example-auth-mock",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "mock": "MOCK_BACKEND_PORT=3102 PROTECTED_DATA_FILE=./data/protected-todos.json node ../example-auth-shared/mock-backend/server.mjs",
    "proxy": "test-proxy-recorder",
    "proxy:reset": "test-proxy-recorder reset",
    "dev": "next dev --port 3100",
    "build": "next build",
    "start": "next start --port 3100",
    "start:all": "concurrently \"pnpm mock\" \"pnpm proxy\" \"pnpm start\"",
    "test:e2e": "pnpm build && concurrently --kill-others --success first --names services,tests \"pnpm start:all\" \"wait-on http://127.0.0.1:3100 http://127.0.0.1:8100/__control && APP_PORT=3100 playwright test --retries 1 && node e2e/assert-redactions.mjs\"",
    "test:e2e:ci": "pnpm build && concurrently --kill-others --success first --names services,tests \"pnpm start:all\" \"wait-on http://127.0.0.1:3100 http://127.0.0.1:8100/__control --timeout 120000 && APP_PORT=3100 RECORD_MODE=1 playwright test --workers 1 && node e2e/assert-redactions.mjs && APP_PORT=3100 playwright test --retries 1\"",
    "test:e2e:ui": "APP_PORT=3100 playwright test --ui --retries 1",
    "test:e2e:record": "APP_PORT=3100 RECORD_MODE=1 playwright test --workers 1 --ui"
  },
  "dependencies": {
    "example-auth-shared": "workspace:*",
    "next": "^16.2.4",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "concurrently": "^9.0.0",
    "test-proxy-recorder": "workspace:*",
    "typescript": "^5.6.0",
    "wait-on": "^8.0.0"
  }
}
